creation_rules:
  - path_regex: secrets/.*\.env$
    age: >-
      age1a0vfsdw4crksldhymh8e4qklmfrsqj5hque4zzyna4zj02k5syvq37tah0
    # Steel's own age key pair (independent from nexo's). Private key lives
    # only as the SOPS_AGE_KEY GitHub Actions secret — never committed.
